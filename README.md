# Wagtail Demo Site

## Installation
```bash
# Install dependencies (Optional: Install and activate a virtual env)
$ pip install -r mysite/requirements.txt

# Run migrations
$ python3 mysite/manage.py migrate

# Create a super user
$ python3 mysite/manage.py createsuperuser

# Run server
$ python3 mysite/manage.py runserver
```
